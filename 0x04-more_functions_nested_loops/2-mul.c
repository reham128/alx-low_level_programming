#include "main.h"

/**
 * mul -  function that multiplies two integers.
 *
 * @a: first input
 * @b: second input
 *
 * Return: return product.
 */
int mul(int a, int b)
{
	product = a * b;
	return (product);
}
