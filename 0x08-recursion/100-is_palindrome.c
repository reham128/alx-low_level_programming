#include "main.h"
/**
 * is_palindrome - a function check if a string is  palindrome or not.
 *
 * @s: input string.
 *
 * Return: 1 if string is a  palindrome and 0 if not.
*/
int is_palindrome(char *s)
{
}
